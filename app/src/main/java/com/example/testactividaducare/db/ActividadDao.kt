package com.example.testactividaducare.db
/*
import androidx.room.Query
import com.example.testactividaducare.entity.Actividad

abstract class ActividadDao:BaseDao<Actividad> {
    @Query("SELECT * FROM Actividad")
    suspend abstract fun getAll() : List<Actividad>

    @Query("SELECT * FROM actividad WHERE id = :pid")
    suspend abstract fun findById(pid : Int) : Actividad
}*/